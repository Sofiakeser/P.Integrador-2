<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('partials/head') %>
<body>
	<!-- Include Header -->
	<%- include('partials/header') %>
	
	<!-- product detail -->
	<div class="container products-wrapper">
		<div class="d-flex align-items-center">
			<h2 class="products-title"><%= datos.nombre %></h2> 
			<p class="product-detail-description">
				<a href="/product-add">Editar</a>
			</p>
		</div>
		<div class="product-detail">
			<div class="row">
				<article class="col-12 col-lg-8 product-detail-info">
					<img src="<%= datos.imagen_producto%>" alt="" class="product-detail-img"> 
					<p class="product-detail-description">
						<%= datos.descripcion %> </p>
				</article>
				<div class="col-12 col-lg-4">	
					<section class="comments">

						<% for( let i = 0; i < datos.comentarios.length; i++ ) { %>
							<article>
								<img src="<%= datos.comentarios[i].usuarios.foto_perfil%>" alt="foto perfil">
								<div class="data">	
									<p class="user">
									<strong> <a href="/profile/<%= datos.comentarios[i].usuarios.id %>"><%= datos.comentarios[i].usuarios.usuario %>:</a> </strong>   <!-- cambiamos --> 
									<%= datos.comentarios[i].comentario %></p>
								</div>
							</article>
						<% } %>

					</section>
					<form class="form" action="/productos/<%= datos.id %>" method="post">
						<input class="form-input" type="text" name="comentar" value="" placeholder="Agregar comentario">
						<button type="submit" class="buy-now-button">AGREGAR COMENTARIO</button> 
					</form>
					<hr>
				</div>
			</div>
		</div>
	</div>
	<!-- /product detail -->

	<!-- Include footer -->
	<%- include('partials/footer') %>

</body>	
</html>