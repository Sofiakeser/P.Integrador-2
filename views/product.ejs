<!DOCTYPE html>
<html lang="es">
<!-- Include Head -->
<%- include('partials/head') %>
<body>
	<!-- Include Header -->
	<%- include('partials/header') %>
	
	<!-- product detail -->
	<div class="container products-wrapper">
		<div class="d-flex align-items-center">
			<h2 class="products-title"><%= productos.nombre %></h2> 
			<p class="product-detail-description">
				<a href="/product-add">Editar</a>
			</p>
		</div>
		<div class="product-detail">
			<div class="row">
				<article class="col-12 col-lg-8 product-detail-info">
					<img src="<%= productos.imagen%>" alt="" class="product-detail-img">
					<p class="product-detail-description">
						<%= productos.descripcion %> </p>
				</article>
				<div class="col-12 col-lg-4">
					<section class="comments">
						<article>
							<img src="<%= productos.comentarios.img%>" alt="">
							<div class="data">
								<p class="user">
								<strong> <a href="/profile"><%= productos.comentarios.nombre %></a> </strong>
								<%= productos.comentarios.comentario %></p>
							</div>
						</article>
						<article>
							<img src="<%= productos.comentarios.img %>" alt="">
							<div class="data">
								<p class="user">
									<strong> <a href="/profile"><%= productos.comentarios.nombre %></a> </strong>
									<%= productos.comentarios.comentario %></p>
							</div>
						</article>
						<article>
							<img src="<%= productos.comentarios.img%>" alt="">
							<div class="data">
								<p class="user">
									<strong> <a href="/profile"><%= productos.comentarios.nombre %></a> </strong>
									<%= productos.comentarios.comentario %></p>
							</div>
						</article>
						<article>
							<img src="<%= productos.comentarios.imagen%>" alt="">
							<div class="data">
								<p class="user">
									<strong> <a href="/profile"><%= productos.comentarios.nombre %></a> </strong>
									<%= productos.comentarios.texto %></p>
							</div>
						</article>
						<article>
							<img src="<%= productos.comentarios.imagen%>" alt="">
							<div class="data">
								<p class="user">
									<strong> <a href="/profile"><%= productos.comentarios.nombre %></a> </strong>
									<%= productos.comentarios.texto %></p>
							</div>
						</article>

					</section>
					<form class="form" action="" method="POST">
						<input class="form-input" type="text" value="" placeholder="Agregar comentario">
						<button type="submit" class="buy-now-button">AGREGAR COMENTARIO</button> <!-- CHECKEAR CUANDO HAGAMOS RUTAS -->
					</form>
					<hr>
				</div>
			</div>
		</div>
	</div>
	<!-- /product detail -->

	<!-- Include footer -->
	<%- include('partials/footer') %>

</body>	
</html>